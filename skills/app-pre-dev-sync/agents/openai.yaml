interface:
  display_name: "App Pre-Dev Sync"
  short_description: "Load repo + Obsidian context before coding"
  default_prompt: "Use $app-pre-dev-sync as the default entry for app repository work to summarize repo context, current working state, and matching Obsidian pages from the repo AGENTS.md."
