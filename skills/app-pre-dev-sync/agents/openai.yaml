interface:
  display_name: "App Pre-Dev Sync"
  short_description: "Load the current app repository context before coding"
  default_prompt: "Use $app-pre-dev-sync to summarize the current app repository context, using the repo AGENTS.md and matching Obsidian pages."
